@model TechStore.Models.ViewModels.CreateCategoryViewModel

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>CreateCategoryViewModel</h4>
    <hr />
    <dl class="dl-horizontal ">
        <dt>
            @Html.DisplayNameFor(model => model.CategoryID)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CategoryID)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
        <div id="products-for-category">
            @foreach(var prod in Model.products)
            {
                @Html.ActionLink(prod.Name, "Details", "Product", new { id = prod.ProductID }, new { @class="btn btn-info"});
            }
        </div>
   
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id= Model.CategoryID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
